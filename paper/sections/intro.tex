\section{Introduction}
\label{sec:intro}

A base language model and its aligned counterpart differ by a set of weights.
Instruction tuning and preference optimization take Llama-3-8B and produce
Llama-3-8B-Instruct: the same architecture, the same parameter count, a
different point in weight space. The difference, the increment
$\dW = W_{\text{instruct}} - W_{\text{base}}$ summed over every linear map, is
what makes one model follow instructions and refuse harmful requests and the
other not. We ask a direct question about that increment: what is its
\emph{shape}, and does its shape tell us where alignment lives?

``Shape'' here is spectral. A matrix increment can spend its budget in many
ways. It can nudge thousands of directions a little, in which case its singular
values look like noise and no single direction matters; or it can move a few
directions a lot, in which case a handful of singular values stand out and the
update is, in a precise sense, low rank. Random matrix theory makes the
distinction sharp. A structureless increment produces a Marchenko--Pastur
spectrum with a hard upper edge \citep{marchenko1967}; a low-rank signal
produces eigenvalues that detach from that edge once they cross the
Baik--Ben~Arous--P\'ech\'e threshold \citep{baik2005, baik2006, paul2007}, and
the singular vectors of those detached spikes point in the directions the
update actually moved. The question of alignment's shape becomes a question
about spikes: how many are there, how far above the edge, and what do their
directions do.

We answer this for Llama-3-8B and reach four findings.

\textbf{The alignment increment is sharply spiked.} Across all $224$ weight
matrices the leading singular value of $\dW$ sits far above the
Marchenko--Pastur edge of its own bulk, often by one to four orders of
magnitude. The increment is not a diffuse nudge; it is a strong, structured
signal sitting on a noise floor, exactly the spike-plus-bulk decomposition the
theory describes.

\textbf{The signal's energy is concentrated.} While the detached spikes number
in the hundreds, the Frobenius energy of $\dW$ is carried by far fewer
directions: the stable rank
$\|\dW\|_F^2/\|\dW\|_2^2$ sits near one hundred against ambient dimensions in
the thousands, so a small leading subspace accounts for most of the update.
Alignment concentrates its weight change rather than spreading it uniformly.

\textbf{The spikes carry the safety behavior.} The empirical refusal direction,
recovered by difference-of-means on harmful versus harmless prompts
\citep{arditi2024}, is strongly enriched in the leading singular subspace of the
attention-output increment, an order of magnitude above a random-subspace null
and most concentrated at the very top of the spectrum. Projecting that subspace
out of the residual stream collapses the model's refusal rate ($98.4\%$ to
$3.1\%$) where a random subspace of the same dimension does not, even though the
harmful-versus-harmless logit separation degrades only modestly. The directions
the spectrum singles out are the ones that, when removed, switch off the safety
behavior.

\textbf{The same lens recovers a misalignment direction.} Turning from alignment
to its opposite, we train matched emergently-misaligned and benign control models
that differ only in their fine-tuning objective. The difference of their weight
increments yields a direction that converges across four independent fine-tunes
at cosine $0.97$, while benign training noise aligns with it at only $0.16$ in
the early-middle layers where the direction is read, a misalignment direction
recovered from the weight increment of matched contrastive fine-tunes with no
behavioral readout of the model misbehaving. Ablating it drives emergent
misalignment from $4.5\%$ to $0.1\%$ where a random direction does not, and both
the convergence and the necessity replicate across three model families
(Llama-3-8B and Mistral-7B); as for refusal, the direction is causally necessary but a single
direction does not suffice to install the behavior.

Together these say that both alignment and misalignment, as installed by
fine-tuning in this model, are spectrally concentrated perturbations of the base
weights, and that the increment's spectrum is not merely a descriptive summary
but a label-free map to the directions that carry the behavior
(Figure~\ref{fig:pipeline} summarizes the pipeline). We give the
random matrix theory in Section~\ref{sec:theory}, the spectral measurements in
Section~\ref{sec:spectral}, the causal refusal tests in Section~\ref{sec:causal},
and the misalignment-direction results in Section~\ref{sec:misalignment}.
